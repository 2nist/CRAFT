# Craft Tools Hub - Environment Configuration
# Copy this file to .env and update with your actual values

# ============================================
# SQL Database Sync Configuration (Optional)
# ============================================
# Configure these if you want to sync with an external SQL Server database

# API endpoint for SQL Server sync
# Example: http://localhost:3000/api or https://api.yourcompany.com/api
REMOTE_API_URL=http://localhost:3000/api

# API authentication key
# Generate a secure random key for production
REMOTE_API_KEY=your-secret-api-key-here

# ============================================
# Application Settings
# ============================================

# Environment mode (development, production, test)
NODE_ENV=development

# ============================================
# SQL Server Database (for server-setup/)
# ============================================
# These are only needed if you're running the included API server

# SQL Server connection details
DB_USER=your_sql_username
DB_PASSWORD=your_sql_password
DB_SERVER=192.168.1.50
DB_NAME=CraftAutomation

# API Server Settings
API_KEY=your-api-server-key
PORT=3000

# ============================================
# Optional Integrations
# ============================================

# Pipedrive CRM Integration (optional)
# PIPEDRIVE_API_KEY=your_pipedrive_api_key
# PIPEDRIVE_DOMAIN=your-domain

# Email Service (optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password

# ============================================
# Development Settings
# ============================================

# Enable debug logging
# DEBUG=true

# Development server port
# DEV_SERVER_PORT=5174

# ============================================
# Security Notes
# ============================================
# - NEVER commit this file with real credentials to version control
# - Use strong, unique passwords and API keys
# - Rotate keys regularly
# - Use HTTPS in production
# - Consider using environment-specific .env files (.env.production, .env.development)
